#include <stdio.h>
/**
* main - Entry point
*
*Description: Print numbers.
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int n;

	for (n = 0 ; n < 10 ; n++)
	{
		if (n == 9)
		{
			putchar(n + '0');
		}
		else
		{
		putchar(i + '0');
		putchar(',');
		putchar(' ');
		}
	}
	return (0);
}
